# tech-debt.yaml — known issues / open follow-ups
UPDATED: "2026-08-21"
STATUS: "OPEN_ITEMS_LOGGED"

ITEMS:
  - id: "cal-apply-verify"
    severity: "medium"
    status: "open"
    detail: "Blob round-trip (cal_apply) and ~/.config/tobii.json persistence after wizard not yet verified end-to-end. Baseline md5 771f74bc78f424a327b7480df0dff7fee."
  - id: "cal-center-offset"
    severity: "low"
    status: "open"
    detail: "Center gaze y ~0.19-0.21 (expected ~0.5); eye z ~670-683mm, eye y ~-99mm in recent calibration captures. Candidate causes: ET5 mounted below bottom bezel (clip mount), tilt 12deg, ultrawide 3440x1440."
  - id: "live-display-area"
    severity: "low"
    status: "open"
    detail: "set_display_area changes require daemon restart to take effect; consider live-apply (avoid restart) for in-game tuning."
  - id: "time-based-capture"
    severity: "low"
    status: "open"
    detail: "Calibration capture windows are sample-count based (60 samples/point); time-based windows might be more robust to low tracker framerate."
  - id: "x4-native-open-arch"
    severity: "low"
    status: "open"
    detail: "ARCHITECTURE.md lists open refactor items: TS Tracker -> Source interface, WsSource TS, SocketSource full daemon protocol (currently gaze-only)."
  - id: "wizard-qa-gate"
    severity: "high"
    status: "open"
    detail: "v0.2.0 not yet through qa-tester gate (zig build test); user manual re-test of 5-point wizard with retry-fix binary pending; then commit/tag/push/release."
  - id: "nfs-build-quirk"
    severity: "medium"
    status: "accepted-workaround"
    detail: "Zig cache atomic rename fails on NFS (/mnt/NAS-Zeno); workaround --cache-dir /tmp/zig-local-cache-* + manual binary copy to zig-out."
  - id: "hibernation-usb"
    severity: "medium"
    status: "accepted-workaround"
    detail: "Post-hibernation USB endpoint stale; daemon restart alone insufficient; USB unplug/replug required, then restart daemon+bridge. Documented in x4-foundations-opentrack.md."