# tech-debt.yaml — OPEN debt ONLY (unresolved / unverified / low-confidence)
# DEBT_POLICY: once an item is confirmed by testing (manual/user preferred),
# move it to .docs/resolved.yaml. Keep items OPEN if only internal/QA-tested
# with low confidence, until verified and really fixed.
UPDATED: "2026-08-30"
STATUS: "OPEN_ITEMS_ONLY"

ITEMS:
  - id: "predictable-config-path"
    severity: "low"
    status: "open"
    detail: "saveToFile writes to predictable ~/.config/tobii.json (same-user symlink attack only; no privilege boundary crossed). Consider O_NOFOLLOW or dir-fd based writes in a hardening pass."
  - id: "live-display-area"
    severity: "low"
    status: "open"
    detail: "set_display_area changes require daemon restart to take effect; consider live-apply (avoid restart) for in-game tuning."
  - id: "time-based-capture"
    severity: "low"
    status: "partially-addressed"
    detail: "Calibration capture windows increased from 60 to 180 samples (~3s at 60Hz) in v0.2.2. Still sample-count based but longer window improves corner re-acquire. Time-based windows remain a future improvement."
  - id: "x4-native-open-arch"
    severity: "low"
    status: "open"
    detail: "ARCHITECTURE.md lists open refactor items: TS Tracker -> Source interface, WsSource TS, SocketSource full daemon protocol (currently gaze-only)."
  - id: "corner-range-inward-jump"
    severity: "medium"
    status: "open"
    detail: "BATCH_2 v1 fixed the corner inward-jump (old drift formula was sign-inverted + mis-scaled; turn_est = -asin(dx/neck) + outward-only gate converges to ~94-98% of equivalent turn, zero static-hold sag). QA-tested only; USER gate pending -> stays OPEN."
  - id: "corner-overshoot-90deg"
    severity: "high"
    status: "open"
    detail: "USER (2026-08-30, real rig): while HOLDING L/R corners the view still creeps to ~90 deg overshoot (exceeds normal head/eye range). ROOT CAUSE (19,934-line v2 trace): v2 raw-only drift bound (anchor + n1_extra_max_deg=15 raw) was tuned head_gain=1.0 + linear curve; real rig edge amplification ~8x (gain 2.0 x Tobii-spline slope ~4 deg/deg near 20-35 input): drift lg 8.5->13-15 raw -> output fy 124-145 deg (lg~9 raw -> ~95 = natural edge). v3+ FIX IMPLEMENTED (output-space cap via invertCurve() + n1_drift_max_out_deg=95; v4.x hold/release rework; v4.6 n=2 corner reach). v4.6 DEPLOYED + LIVE (bridge PID 55808, ReleaseSafe BUILD_INFO 5e7a301+v4.6-corner-reach-release-fix, QA bridge 23/23 + driver 23/23). USER GATE on v4.6 PENDING (not fully re-run before the v0.2.7 release) — stays OPEN until the user confirms: no 90-deg creep while HOLDING corners, upper corners incl LEFT-upper reach the edge, no single-frame pops."
  - id: "gui-eye-jitter"
    severity: "medium"
    status: "open"
    detail: "USER (v0.2.7 release, 2026-08-30): eyes in the GUI visualization constantly move slightly at rest — needs smoothing (tug-path + per-eye). BATCH_4-adjacent; threshold must be DATA-DERIVED (BATCH_3 diagnostics), do NOT hardcode. v4.6 tug_smoothing 0.85->0.88 was a mild reduction only."
  - id: "fast-turn-overshoot"
    severity: "medium"
    status: "open"
    detail: "USER (v0.2.7 release, 2026-08-30): fast left/right head turns make the HEAD-viz dot SHOOT ~50% past the ring then correct back. ROOT-CAUSED in v4.6 trace (frames 3258-3266): One-Euro fc_max = 4 Hz @ smoothing 0.90 cannot track a ~220 deg/s head reversal — smoothed yaw lags ~6-10 frames across center (frame 3258: hp=-4.1 but rw=+24.5, dot points the WRONG way) and the Tobii curve amplifies the stale signal (yaw 5.08 -> rw 35.0 = 48% past the ring edge). EXPERIMENT v4.7 (fc_d 1->3, fc_max_mult 30->150; sim: reversal lag 11.2->6.3 deg, rest jitter unchanged) REVERTED at release — broke BATCH_2i (release-ease step > 6.0 bound). Needs a fixed regression test before re-apply."